DATABASE_URL=postgresql://user:password@host:port/cinema_booking

JWT_SECRET=
JWT_ALGORITHM=HS256

# Debe coincidir con INTERNAL_SERVICE_TOKEN en user-service-cinema y catalog-service-cinema
INTERNAL_SERVICE_TOKEN=

# Kafka (Confluent Cloud)
KAFKA_ENABLED=true
KAFKA_BOOTSTRAP_SERVERS=
KAFKA_API_KEY=
KAFKA_API_SECRET=
# group_id del consumer de Kafka. Dev y prod comparten el mismo cluster de
# Confluent Cloud -- en local usa SIEMPRE un sufijo distinto (ej. "-local")
# para no unirte al mismo grupo de consumidores que produccion.
KAFKA_GROUP_ID=booking-service-group-local

# URLs de microservicios
# Local (Docker):      http://payment-service:8003
# Producción (Render): https://payment-service-cinema.onrender.com
PAYMENT_SERVICE_URL=

# Local (Docker):      http://catalog-service:8006
# Producción (Render): https://catalog-service-cinema.onrender.com
CATALOG_SERVICE_URL=

# Redis
REDIS_URL=redis://user:password@host:port

# Pagos con Wompi: minutos para pagar el enlace (el expiration-time de Wompi;
# los asientos quedan retenidos ese tiempo) y cuánto se espera después un
# resultado tardío (un PSE que empezó a tiempo) antes de cancelar la compra.
PAYMENT_CHECKOUT_TTL_SECONDS=600
PAYMENT_RESULT_GRACE_SECONDS=900

# Precios: General = precio de la película; Preferencial = + recargo (pesos).
# Filas preferenciales separadas por coma. Valor por servicio si hay comida.
PREFERENTIAL_SURCHARGE=5700
PREFERENTIAL_ROWS=K,L,M,N,O,P
CONCESSION_SERVICE_FEE=4800
